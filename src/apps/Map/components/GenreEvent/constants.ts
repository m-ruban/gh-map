import { WIDTH_YEAR } from 'map/modules/constants';

export const PADDING_WRAPPER = 32;
export const PADDING_INFO = 24;
export const GENRE_IMAGE_HEIGHT = 170;
export const ICON_SIZE = 30;
export const MAX_TEXT_WIDTH = WIDTH_YEAR - PADDING_WRAPPER * 2 - PADDING_INFO * 2 - ICON_SIZE - PADDING_INFO;
